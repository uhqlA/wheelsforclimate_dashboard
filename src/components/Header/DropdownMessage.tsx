import ClickOutside from '../ClickOutside';

const DropdownMessage = () => {
  return (
    <ClickOutside onClick={() => false} className="relative">
      <li className="relative">
        {/* <!-- Dropdown Start --> */}

        {/* <!-- Dropdown End --> */}
      </li>
    </ClickOutside>
  );
};

export default DropdownMessage;
